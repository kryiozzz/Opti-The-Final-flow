@{
    ViewData["Title"] = "Home Page";
}

<!-- Link to external CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="~/css/home.css" />

<!-- Hero Section with Image Slideshow Background -->
<div class="hero-section">
    <div class="hero-overlay"></div>

    <!-- Slideshow Background -->
    <div class="slideshow-container">
        <div class="slideshow-image active" style="background-image: url('/images/factory1.jpg')"></div>
        <div class="slideshow-image" style="background-image: url('/HomeImages/photo2.jpg')"></div>
        <div class="slideshow-image" style="background-image: url('/HomeImages/photo3.jpg')"></div>
        <div class="slideshow-image" style="background-image: url('/HomeImages/photo4.jpeg')"></div>
        <div class="slideshow-image" style="background-image: url('/HomeImages/photo5.jpg')"></div>
    </div>

    <!-- Content -->
    <div class="container">
        <div class="hero-content">
            <h1 class="hero-title">Welcome to Opti</h1>
            <p class="hero-subtitle">Your comprehensive manufacturing and production management solution</p>
        </div>
    </div>

    <!-- Slide Navigation Dots -->
    <div class="slideshow-dots">
        <span class="dot active" data-slide="0"></span>
        <span class="dot" data-slide="1"></span>
        <span class="dot" data-slide="2"></span>
        <span class="dot" data-slide="3"></span>
        <span class="dot" data-slide="4"></span>
    </div>
</div>

<!-- Features Section -->
<section class="features-section">
    <div class="container">
        <h2 class="section-title text-center">Our Key Solutions</h2>
        <div class="row">
            <!-- Feature: Production Orders -->
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-clipboard-list"></i>
                    </div>
                    <h3>Production Orders</h3>
                    <p>Streamline your production workflow with our comprehensive order management system.</p>
                </div>
            </div>

            <!-- Feature: Machine Management -->
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-cogs"></i>
                    </div>
                    <h3>Machine Management</h3>
                    <p>Track and manage all your machinery with our detailed logging and monitoring tools.</p>
                </div>
            </div>

            <!-- Feature: Customer Orders -->
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-shopping-cart"></i>
                    </div>
                    <h3>Customer Orders</h3>
                    <p>Efficiently manage customer orders and track their progress from submission to delivery.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Call to Action Section -->
<section class="cta-section">
    <div class="container">
        <div class="cta-content">
            <h2>Ready to Optimize Your Production?</h2>
            <p>Get started with our comprehensive tools and take control of your manufacturing processes.</p>
            <div class="cta-buttons">
                <a asp-controller="Account" asp-action="Register" class="btn btn-light btn-lg">Register Now</a>
            </div>
        </div>
    </div>
</section>

 <hr>

<!-- Footer Section with Modal Integration -->
<footer class="site-footer">
    <div class="container">
        <div class="footer-container">
            <div class="footer-column">
                <h3 class="footer-heading">Quick Links</h3>
                <ul class="footer-links">
                    <li><a href="/">Home</a></li>
                    <li><a href="/Products">Products</a></li> 
                    <li><a href="/Machines">Machines</a></li>
                </ul>
            </div>

            <div class="footer-column">
                <h3 class="footer-heading">Contact Us</h3>
                <ul class="footer-contact">
                    <li><i class="fas fa-map-marker-alt"></i> 123 Business Park, Davao City, Philippines</li>
                    <li><i class="fas fa-phone"></i> +63 912 345 6789</li>
                    <li><i class="fas fa-envelope"></i> info@optiflow.com</li>
                </ul>
            </div>

            <div class="footer-column">
                <h3 class="footer-heading">Newsletter</h3>
                <div class="footer-newsletter">
                    <p>Subscribe to our newsletter for the latest updates and offers.</p>
                    <form class="newsletter-form">
                        <input type="email" class="newsletter-input" placeholder="Your email" required>
                        <button type="submit" class="newsletter-button">→</button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Bottom divider line -->
        <hr>

        <div class="footer-bottom">
            <div class="copyright">
                © 2025 - OptiFlow. All rights reserved.
            </div>
            <div class="footer-bottom-links">
                <a href="#" data-bs-toggle="modal" data-bs-target="#aboutModal">About Us</a> |
                <a href="#" data-bs-toggle="modal" data-bs-target="#cookiesModal">Cookies Policy</a> |
                <a href="#" data-bs-toggle="modal" data-bs-target="#termsModal">Terms & Conditions</a> |
                <a href="#" data-bs-toggle="modal" data-bs-target="#privacyModal">Privacy Policy</a>
            </div>
        </div>
    </div>
</footer>

<!-- ABOUT US MODAL -->
<div class="modal fade" id="aboutModal" tabindex="-1" aria-labelledby="aboutModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="aboutModalLabel">About OptiFlow</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p><strong>OptiFlow</strong> is a leading provider of automation solutions tailored for the industrial and manufacturing sectors. We streamline operations through smart automation systems that optimize productivity, reduce manual errors, and enhance output quality.</p>

                <p>In addition, our Point of Sale (POS) system supports real-time sales transactions, inventory tracking, and customer engagement, making it ideal for business units selling directly to consumers.</p>

                <p>Our mission is to empower industries and retailers with efficient, scalable, and intelligent automation tools.</p>

                <h6>Our Vision</h6>
                <p>To be the global leader in providing intelligent automation solutions that transform manufacturing operations and retail experiences.</p>

                <h6>Our Values</h6>
                <ul>
                    <li><strong>Innovation:</strong> Constantly pushing the boundaries of what's possible</li>
                    <li><strong>Excellence:</strong> Delivering the highest quality in everything we do</li>
                    <li><strong>Integrity:</strong> Operating with honesty and transparency</li>
                    <li><strong>Collaboration:</strong> Working together to achieve extraordinary results</li>
                </ul>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<!-- COOKIES POLICY MODAL -->
<div class="modal fade" id="cookiesModal" tabindex="-1" aria-labelledby="cookiesModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="cookiesModalLabel">Cookies Policy</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>We use cookies to enhance your experience on the OptiFlow platform. Cookies help us remember your preferences, improve performance, and deliver personalized content. By continuing to use our site, you consent to the use of cookies. You may adjust your browser settings to disable cookies, but some features of the platform may not function properly as a result.</p>

                <h6>Types of cookies we use:</h6>
                <ul>
                    <li><strong>Essential cookies</strong> – required for site functionality.</li>
                    <li><strong>Analytics cookies</strong> – used to measure usage and improve performance.</li>
                    <li><strong>Preference cookies</strong> – to save settings like language or region.</li>
                    <li><strong>Marketing cookies</strong> – to deliver more relevant advertisements and track their effectiveness.</li>
                </ul>

                <h6>How to manage cookies</h6>
                <p>Most web browsers allow you to control cookies through their settings. You can:</p>
                <ul>
                    <li>Delete cookies from your device</li>
                    <li>Block cookies by activating the setting on your browser that allows you to refuse all or some cookies</li>
                    <li>Set your browser to notify you when you receive a cookie</li>
                </ul>

                <p>Please note that if you choose to block or delete cookies, certain features of our website may not work correctly.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<!-- TERMS AND CONDITIONS MODAL -->
<div class="modal fade" id="termsModal" tabindex="-1" aria-labelledby="termsModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="termsModalLabel">Terms and Conditions</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>By using OptiFlow's platform and services, you agree to abide by the following terms:</p>

                <h6>1. Acceptance of Terms</h6>
                <p>By accessing or using OptiFlow's services, you agree to be bound by these Terms and Conditions and all applicable laws and regulations. If you do not agree with any of these terms, you are prohibited from using or accessing this site.</p>

                <h6>2. Legal Compliance</h6>
                <p>All automation and POS services are provided in accordance with Republic Act No. 8792 (E-Commerce Act) and relevant Philippine laws.</p>

                <h6>3. User Responsibilities</h6>
                <ul>
                    <li>You must not use the system to upload or distribute unlawful content or interfere with system operations.</li>
                    <li>You are responsible for maintaining the confidentiality of your account information.</li>
                    <li>You must not engage in any activity that could damage, disable, or impair our services.</li>
                </ul>

                <h6>4. Intellectual Property</h6>
                <p>All intellectual property, software, and content belong to OptiFlow and may not be used without permission.</p>

                <h6>5. Limitation of Liability</h6>
                <p>We are not liable for indirect losses resulting from service interruptions or system errors.</p>

                <h6>6. Modifications</h6>
                <p>Terms may be modified at any time, and continued use implies acceptance of updates.</p>

                <h6>7. Governing Law</h6>
                <p>For legal disputes, jurisdiction falls under the courts of Davao City, Philippines.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<!-- PRIVACY POLICY MODAL -->
<div class="modal fade" id="privacyModal" tabindex="-1" aria-labelledby="privacyModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="privacyModalLabel">Privacy Policy</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>OptiFlow is committed to protecting your personal information in compliance with the Data Privacy Act of 2012 (RA 10173).</p>

                <h6>1. Collection and Use</h6>
                <p>We collect personal data such as name, contact info, user activity, and transaction records to support platform functionality, analytics, and communication.</p>

                <h6>2. Data Protection</h6>
                <p>We use encryption, secure servers, and role-based access control to safeguard user data.</p>

                <h6>3. Sharing and Disclosure</h6>
                <p>Data will not be shared with third parties unless required by law or with user consent.</p>

                <h6>4. Your Rights</h6>
                <p>You may access, update, or request deletion of your data, or file complaints with our Data Protection Officer at privacy@optiflow.com.</p>

                <h6>5. Retention Period</h6>
                <p>We will retain your personal information for as long as necessary to fulfill the purposes outlined in this Privacy Policy, unless a longer retention period is required or permitted by law.</p>

                <h6>6. Changes to Privacy Policy</h6>
                <p>We may revise this Privacy Policy and notify users via system message or email. Continued use indicates consent to the new terms.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

    <a href="#" id="backToTop" class="back-to-top">
        <i class="fas fa-arrow-up"></i>
    </a>
</footer>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Back to top button functionality with smooth transition
    document.addEventListener('DOMContentLoaded', function() {
        const backToTopButton = document.getElementById('backToTop');

        if (!backToTopButton) return; // Exit if button doesn't exist

        // Add transition styles directly via JavaScript
        backToTopButton.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
        backToTopButton.style.opacity = '0';
        backToTopButton.style.transform = 'translateY(20px)';
        backToTopButton.style.pointerEvents = 'none'; // Prevent clicking when invisible

        // Handle scroll events with throttling for better performance
        let isScrolling;
        window.addEventListener('scroll', () => {
            // Clear the timeout on each scroll event
            window.clearTimeout(isScrolling);

            // Set a timeout to run after scrolling ends
            isScrolling = setTimeout(() => {
                if (window.pageYOffset > 300) {
                    backToTopButton.style.opacity = '1';
                    backToTopButton.style.transform = 'translateY(0)';
                    backToTopButton.style.pointerEvents = 'auto'; // Enable clicking
                } else {
                    backToTopButton.style.opacity = '0';
                    backToTopButton.style.transform = 'translateY(20px)';
                    backToTopButton.style.pointerEvents = 'none'; // Disable clicking
                }
            }, 50); // Small timeout for performance
        });

        // Smooth scroll to top with easing
        backToTopButton.addEventListener('click', (e) => {
            e.preventDefault();

            // Get the starting position
            const startPosition = window.pageYOffset;
            const duration = 800; // Duration in milliseconds
            let startTime = null;

            // Animation function with easing
            function animation(currentTime) {
                if (startTime === null) startTime = currentTime;
                const timeElapsed = currentTime - startTime;
                const progress = Math.min(timeElapsed / duration, 1);

                // Easing function: easeOutQuad
                const ease = 1 - Math.pow(1 - progress, 2);

                window.scrollTo(0, startPosition * (1 - ease));

                if (timeElapsed < duration) {
                    requestAnimationFrame(animation);
                }
            }

            // Start the animation
            requestAnimationFrame(animation);
        });
    });

    // Optional: Enhanced parallax effect on mouse movement
    document.addEventListener('DOMContentLoaded', function() {
        const heroSection = document.querySelector('.hero-section');
        const parallaxBg = document.querySelector('.parallax-bg');

        heroSection.addEventListener('mousemove', function(e) {
            const x = e.clientX / window.innerWidth;
            const y = e.clientY / window.innerHeight;

            parallaxBg.style.transform = `translateY(-${y * 20}px) translateX(-${x * 10}px)`;
        });

        // Reset position when mouse leaves
        heroSection.addEventListener('mouseleave', function() {
            parallaxBg.style.transform = '';
        });
    });

     document.addEventListener('DOMContentLoaded', function() {
        // Slideshow functionality
        const slides = document.querySelectorAll('.slideshow-image');
        const dots = document.querySelectorAll('.dot');
        let currentSlide = 0;
        const slideInterval = 5000; // Change slide every 5 seconds

        // Function to change slide
        function changeSlide(n) {
            // Remove active class from current slide and dot
            slides[currentSlide].classList.remove('active');
            dots[currentSlide].classList.remove('active');

            // Update current slide index
            currentSlide = (n + slides.length) % slides.length;

            // Add active class to new slide and dot
            slides[currentSlide].classList.add('active');
            dots[currentSlide].classList.add('active');
        }

        // Auto slideshow
        let slideTimer = setInterval(() => {
            changeSlide(currentSlide + 1);
        }, slideInterval);

        // Click handlers for dots
        dots.forEach((dot, index) => {
            dot.addEventListener('click', () => {
                // Clear the interval and restart it
                clearInterval(slideTimer);
                changeSlide(index);
                slideTimer = setInterval(() => {
                    changeSlide(currentSlide + 1);
                }, slideInterval);
            });
        });

        // Pause slideshow on hover (optional)
        const heroSection = document.querySelector('.hero-section');
        heroSection.addEventListener('mouseenter', () => {
            clearInterval(slideTimer);
        });

        heroSection.addEventListener('mouseleave', () => {
            slideTimer = setInterval(() => {
                changeSlide(currentSlide + 1);
            }, slideInterval);
        });
    });

    // Ensure the modals are properly initialized
document.addEventListener('DOMContentLoaded', function() {
    // Initialize Bootstrap modals
    var modals = [
        document.getElementById('aboutModal'),
        document.getElementById('cookiesModal'),
        document.getElementById('termsModal'),
        document.getElementById('privacyModal')
    ];

    modals.forEach(function(modal) {
        if (modal) {
            new bootstrap.Modal(modal);
        }
    });

    // Newsletter form submission
    const newsletterForm = document.querySelector('.newsletter-form');
    if (newsletterForm) {
        newsletterForm.addEventListener('submit', function(event) {
            event.preventDefault();
            const emailInput = this.querySelector('.newsletter-input');
            const email = emailInput.value.trim();

            if (email) {
                // Here you would normally send this to your backend
                alert('Thank you for subscribing with: ' + email);
                emailInput.value = '';
            }
        });
    }
});
</script>